# Gallery Images

- General gallery images
- Spa atmosphere photos
- Mixed photos that do not belong to a more specific category

When real photos are ready, add them to this folder and update `src/data/galleryImages.ts` with only:

- `src`: imported image path
- `alt`: accessible image alt text
